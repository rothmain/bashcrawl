enturer on the floor of the
dungeon.  You find the following items on the corpse: 

${I}

EOF
    curr=$(basename $PWD)
    while [ "$curr" != "entrance" ]; do
        pushd .. &> /dev/null
        dirs -c
        curr=$(basename $PWD)
    done
    unset I
    unset HP
    $SHELL
}

resolve() {
    if ! grep --quiet --only-matching sword <<< $I; then
        let NUM=NUM-5
    fi
    if [[ $MONSTER -eq $NUM ]]; then
        cat << EOF
You dodge an attack, but you miss your mark as a result.
Roll again...

EOF
	roll
    fi
    
    if [[ $MONSTER -lt $NUM ]]; then
        cat << EOF
A hit! A palpable hit!  The robot falls back to pieces.
Before it does, a spell eminates from what looks like its
mouth.

EOF
       cat << EOF > spell 
       
INCREASE YOUR VISION!  The spell you use 
to illuminate this place ('ls') has many 
more options that can alter what